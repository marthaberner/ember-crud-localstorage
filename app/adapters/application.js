import DS from 'ember-data';

export default DS.LSAdapter.extend({
  // host: "http://localhost:3000/"
});

// ActiveModelAdapter - more specific version just for rails

// RESTAdapter is for any api
